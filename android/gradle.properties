org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
# Force Gradle to use the JDK bundled with Android Studio (Java 21/JBR),
# because some newer system JDKs (e.g. JDK 25) may be incompatible with
# the Kotlin/Gradle toolchain used here. Adjust the path if your Android
# Studio is installed elsewhere.
org.gradle.java.home=C:\\Program Files\\Android\\Android Studio\\jbr
